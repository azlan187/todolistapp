<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
    <%= f.label :importance %>
    <%= f.select(:importance, ["Important","Not Important"]) %>
    <%= f.label :urgency %>
    <%= f.select(:urgency, ["Urgent", "Not Urgent"]) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
